# Copy to .env and fill in values before running docker compose.

# PostgreSQL password (used by both api and db services).
POSTGRES_PASSWORD=changeme

# HS256 JWT signing secret. Minimum 32 characters. Must match the value
# used to generate tokens for the /api/* endpoints.
JWT_SECRET=dev-secret-key-change-in-production-must-be-32-chars
